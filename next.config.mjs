/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/portfolio',
  assetPrefix: '/portfolio/',
  trailingSlash: true, // helps GitHub Pages serve static files correctly
};

export default nextConfig;
